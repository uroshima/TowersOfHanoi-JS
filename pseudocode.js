const run = function() => {
  // 1.until all the rings are in an order (starting with the
  // biggest one at the bottom and smallest at the top) on one of the
  // towers (other than the starting one)
  // 2.get a move from the player
  // 3.make the move if its valid or return error if its not 
  //
}
witch current player
